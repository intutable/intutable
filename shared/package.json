{
    "name": "shared",
    "version": "0.1.0",
    "description": "source code shared across frontend and backend.",
    "private": true,
    "scripts": {
        "build": "tsc",
        "lint": "cross-env NODE_ENV=development eslint src",
        "lint:fix": "npm run lint -- --fix",
        "lint:production": "cross-env NODE_ENV=production eslint src",
        "test": "jest"
    },
    "devDependencies": {
        "@types/jest": "^29.4.0",
        "jest": "^29.4.3",
        "ts-jest": "^29.0.5"
    }
}
