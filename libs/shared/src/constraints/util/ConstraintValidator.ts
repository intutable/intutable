import { ConstraintObjectNotation as Constrain, IfObjectNotation as If } from "./ObjectNotation"

class ConstrainValidation {
    private state = ""

    constructor(public readonly constrain: Constrain) {}

    private evalute(condition: If) {}

    // private async fetchConstraintContextProps(): Promise<ConstraintContextProps> {}
}
