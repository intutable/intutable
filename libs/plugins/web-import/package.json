{
    "name": "@intutable-org/web-import",
    "version": "0.1.0",
    "description": "",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc --build",
        "clean": "tsc --build --clean",
        "lint": "npx eslint \"src/**\"",
        "test-ci": "npm run build && npx jest --color=true",
        "test": "docker-compose build && docker-compose up --abort-on-container-exit",
        "dev": "npx tsc --watch & npx nodemon",
        "format": "npx prettier \"src/**\" --check"
    },
    "dependencies": {
        "@intutable-org/core": "*",
        "@intutable-org/database": "*",
        "@intutable-org/lazy-views": "*",
        "@intutable-org/project-management": "*",
        "axios": "^1.1.3",
        "cheerio": "^1.0.0-rc.12"
    },
    "devDependencies": {
        "@intutable-org/eslint-config": "*",
        "@types/cheerio": "^0.22.31",
        "@types/jest": "^29.2.2",
        "jest": "^29.3.1",
        "nodemon": "^2.0.20",
        "prettier": "3.x",
        "ts-jest": "^29.0.3",
        "ts-node": "^10.9.1",
        "typescript": "^4.8.4"
    }
}
