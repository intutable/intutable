import { PersonHandle } from "../scrapers/resources"

/** Filters duplicates by the persons pids. */
export const filterDuplicates = (persons: PersonHandle[]): PersonHandle[] => {
    return persons
}
