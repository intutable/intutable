{
    "name": "@intutable-org/http",
    "version": "0.1.0",
    "description": "Plugin for http endpoints in intutable",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc --build",
        "clean": "tsc --build --clean",
        "format": "npx prettier \"src/**\" --check",
        "lint": "npx eslint \"src/**\"",
        "test": "npm run build && npx jest"
    },
    "dependencies": {
        "@intutable-org/core": "*",
        "express": "^4.17.1"
    },
    "devDependencies": {
        "@intutable-org/eslint-config": "*",
        "@types/jest": "^27.0.2",
        "axios": "^0.22.0",
        "jest": "^27.2.4",
        "ts-jest": "^27.0.5",
        "typescript": "^4.4.3",
        "@types/express": "^4.17.13"
    }
}
