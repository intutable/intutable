{
    "name": "@intutable-org/mail-plugin",
    "version": "0.1.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc --build",
        "clean": "tsc --build --clean",
        "lint": "npx eslint \"src/**\"",
        "test": "npm run build && npx jest",
        "format": "npx prettier \"src/**\" --check"
    },
    "dependencies": {
        "@intutable-org/core": "*",
        "nodemailer": "^6.9.1"
    },
    "devDependencies": {
        "@intutable-org/eslint-config": "*",
        "@types/jest": "^27.0.2",
        "@types/nodemailer": "^6.4.7",
        "jest": "^27.2.4",
        "prettier": "3.x",
        "ts-jest": "^27.0.5",
        "typescript": "^4.4.3"
    }
}
