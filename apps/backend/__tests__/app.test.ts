import { app } from "../src/app"

// test("the intutable backend app should start without crashing", () => {
//     expect(async () => {
//         const core = await app()

//         // core.stop() // TODO: core is missing a method to stop the server
//     }).not.toThrow()
// })
